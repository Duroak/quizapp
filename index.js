let questionNum = 1;
let answer = 0;

// working on my template literal still
function generateTemplate (correctAnswer, question) {
    return `
    <main role="main">
    <h1 class="question">${question}</h1>

    <form>
        <fieldset>
            <label>
                <input class="answer></input>
                <span>${}</span>
            </label>
    `
}

//not done with this function
function handleStartButton() {
    $('.startButton').click(function(event){

    });
}